namespace Capstone.Repositories
{
    public interface INotificationRepository
    {
        // Hiển thị thông báo khi CV được xem, được lưu, được apply (user)
        // Hiển thị thông báo khi gửi CV cho 1 JD (recruiter)
    }
}
